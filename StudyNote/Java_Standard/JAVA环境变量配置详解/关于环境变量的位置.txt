环境变量的布局分为两种，间java环境变量形式
如何测试java环境？ -------cmd java测试是否存在环境     cmd java --version输出java版本号  cd --进入文件夹  			cd..  ---回退至上一文件夹



需要注意的点：
	我们这里配置的是运行时环境（运行类型），对应我们新建springInitailize的SDK
	（；´д｀）ゞ：另外，在新建springInitialize文件时的Java选项指的是运行的语法格式

为什么我们要在这里提及javac指令？
	（；´д｀）ゞ: 为了表现出配置环境变量的好处
	 如果我们未配置环境变量，cmd指令执行java文件就只能在jdk文件中的bin文件夹中，配置环境变量后则可以在各个文件夹中执行java文件